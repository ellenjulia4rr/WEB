const [a, b, c, d,] = [6, 2, 4,5]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = b * d
const divisao = a / b
const modulo = d % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)